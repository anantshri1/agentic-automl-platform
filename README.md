# agentic-automl-platform
An end-to-end machine learning system that automates the full lifecycle of tabular ML workflows using an LLM-driven orchestration layer. This system decomposes the ML pipeline into modular, composable tools exposed via an MCP server and orchestrated through a LangGraph-based agent.
