fastapi == 0.115.0          # web framework for building APIs
uvicorn == 0.30.6           # server for running FastAPI applications
python-multipart == 0.0.9   # needed for file uploads in FastAP
pydantic == 2.8.2           # data validation library used by FastAPI
mcp[cli] >= 1.3.0            # MCP library with CLI support
httpx                       # async HTTP client for Python
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost==2.1.4
catboost>=1.2.0
tensorflow==2.19.1